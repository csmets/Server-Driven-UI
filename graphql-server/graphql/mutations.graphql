type Mutation {
    save(feedId: String): MutationResponse
}

type Mutation {
    unsave(feedId: String): MutationResponse
}

type Mutation {
    updateHeading(heading: String): MutationResponse
}

type MutationResponse {
    signals: [EmitSignal!]
}