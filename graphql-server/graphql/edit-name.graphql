union FormElement = Button | TextInput

type EditNameContainer {
    elements: [FormElement!]
}

type TextInput {
    formId: String!
    placeholder: String
}

type Button {
    label: String!
    action: Action!
}
