type Signal {
    signalId: String
    states: [State!]
}

type State {
    key: StateKey!
    value: String
}

enum StateKey {
    OK
    ERROR
}

type EmitSignal {
    signalId: String
    key: StateKey
}