type Signal {
    signalId: String
    states: [State!]
}

type State {
    key: StateKey!
    value: String
}

enum StateKey {
    SAVED
    UNSAVED
    UPDATED
    ERROR
}

type EmitSignal {
    signalId: String
    key: StateKey
    value: String
}