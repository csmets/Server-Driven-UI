mutation saveItem($feedId: String) {
    save(feedId: $feedId) {
        signals {
            signalId
            key
        }
    }
}

mutation unsaveItem($feedId: String) {
    unsave(feedId: $feedId) {
        signals {
            signalId
            key
        }
    }
}